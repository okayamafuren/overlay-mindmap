# Overlay Mind Map - 디자인 문서

## 프로젝트 개요
오버레이 마인드맵 애플리케이션은 다른 애플리케이션 위에 표시되는 투명한 마인드맵 도구입니다. 기억술(Mnemonics) 학습을 위한 특화 기능을 포함하고 있습니다.

## 주요 기능

### 1. 데이터 영속성 ✅
- **파일 저장/불러오기**: JSON 형식으로 마인드맵 데이터 저장 및 불러오기
- **자동 저장**: 30초마다 자동으로 임시 파일에 저장
- **최근 파일**: 앱 실행 시 마지막 작업 파일 자동 불러오기
- **단축키**: 
  - `Ctrl+S`: 저장
  - `Ctrl+O`: 열기

### 2. 다른 앱 사용 가능 (마우스 통과) ✅
- **마우스 통과 모드**: `P` 키 또는 툴바 버튼으로 활성화
- 활성화 시 마인드맵을 클릭해도 아래 앱과 상호작용 가능
- 다시 누르면 마인드맵 조작 가능 상태로 복귀

### 3. 암기 모드 (Memorization Mode) ✅
- **활성화**: `M` 키 또는 툴바 버튼
- 모든 노드 텍스트가 물음표(?)로 가려짐
- 노드에 마우스 호버 또는 클릭 시에만 내용 표시
- 능동적 회상(Active Recall) 훈련 가능
- 캔버스 테두리에 빨간색/노란색 강조 효과

### 4. 기본 기능
- 노드 추가/삭제/편집
- 노드 간 연결
- 드래그 앤 드롭
- 줌 인/아웃 (마우스 휠, +/- 키)
- 팬 (중간 버튼 또는 Ctrl+드래그)
- 실행 취소/다시 실행 (Ctrl+Z/Y)
- 검색 기능
- 다양한 노드 속성 (색상, 크기, 모양, 태그, 메모, 링크 등)

### 5. 고급 기능
- 다양한 레이아웃 (트리, 원형, 그리드, 힘 기반)
- 노드 그룹화
- 정렬 기능
- 복사/붙여넣기
- 통계 및 분석
- 50가지 이상의 추가 기능

## UI 컴포넌트

### 상단 툴바
- 왼쪽: 추가, 연결, 삭제, 실행취소/다시실행, 저장, 열기
- 중간: 줌 컨트롤, 검색
- 오른쪽: 마우스 통과, 암기 모드, 색상, 테마, 설정, 최소화, 종료

### 노드 속성 패널
- 텍스트, 색상, 크기, 폰트 크기
- 모양 (원형, 사각형, 다이아몬드, 육각형)
- 테두리, 투명도
- 아이콘, 메모, 태그
- 우선순위, 진행률
- 링크

### 상태 표시줄
- 상태 메시지
- 노드 개수
- 연결 개수
- 마우스 위치

## 단축키 목록

### 기본 조작
- `A`: 노드 추가
- `C`: 연결 모드
- `Delete/Backspace`: 삭제
- `F`: 맞춤 보기
- `ESC`: 종료
- `P`: 마우스 통과 토글
- `M`: 암기 모드 토글

### 편집
- `Ctrl+Z`: 실행 취소
- `Ctrl+Y`: 다시 실행
- `Ctrl+S`: 저장
- `Ctrl+O`: 열기
- `Ctrl+C`: 복사
- `Ctrl+V`: 붙여넣기
- `Ctrl+A`: 전체 선택

### 줌/네비게이션
- `+/-`: 줌 인/아웃
- `0`: 줌 리셋
- `Shift+화살표`: 노드 이동

### 레이아웃
- `Ctrl+L`: 힘 기반 레이아웃
- `Ctrl+1`: 트리 레이아웃
- `Ctrl+2`: 원형 레이아웃
- `Ctrl+3`: 그리드 레이아웃

### 그룹화
- `Ctrl+G`: 그룹화
- `Ctrl+Shift+G`: 그룹 해제

## 기술 스택

- **Electron**: 데스크톱 애플리케이션 프레임워크
- **HTML5 Canvas**: 그래픽 렌더링
- **JavaScript (ES6+)**: 로직 구현
- **CSS3**: 스타일링 (글래스모피즘 효과)

## 파일 구조

```
overlay-mindmap/
├── main.js          # Electron 메인 프로세스
├── renderer.js      # 렌더러 프로세스 (UI 로직)
├── index.html       # HTML 구조
├── style.css        # 스타일시트
└── DESIGN.md        # 디자인 문서 (이 파일)
```

## 데이터 형식

### 저장 파일 (JSON)
```json
{
  "nodes": [...],
  "connections": [...],
  "nodeGroups": [...],
  "version": "2.0",
  "timestamp": 1234567890,
  "zoom": 1.0,
  "panX": 0,
  "panY": 0
}
```

## 보안 고려사항

현재 버전에서는 빠른 개발을 위해 `nodeIntegration: true`와 `contextIsolation: false`를 사용하고 있습니다. 프로덕션 배포 전에는 다음과 같은 보안 강화가 필요합니다:

1. `contextIsolation: true` 활성화
2. `preload.js`를 통한 안전한 IPC 통신
3. CSP (Content Security Policy) 적용
4. 외부 리소스 로딩 제한

## 성능 최적화

1. **Canvas 최적화**
   - 필요한 영역만 다시 그리기 (더티 체킹)
   - 노드가 많을 때 레벨 오브 디테일 (LOD) 적용
   - 렌더링 프레임률 제한

2. **메모리 관리**
   - 히스토리 제한 (50단계)
   - 사용하지 않는 데이터 정리

3. **이벤트 처리**
   - 디바운싱 및 쓰로틀링 적용
   - 이벤트 리스너 최적화

## 향후 개선 사항

- [ ] 보안 강화 (contextIsolation 활성화)
- [ ] 성능 최적화 (가상화, 더티 체킹)
- [ ] 테마 시스템 확장
- [ ] 플러그인 시스템
- [ ] 클라우드 동기화
- [ ] 협업 기능

## 라이선스

이 프로젝트는 기억술 학습을 위한 오버레이 마인드맵 도구입니다.
